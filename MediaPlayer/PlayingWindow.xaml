<UserControl x:Class="MediaPlayer.PlayingWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MediaPlayer"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="500">
    <Grid Width="500px" Height="250px">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <Label Grid.Column="0" Grid.Row="0" Content="File Name:" FontSize="16" FontWeight="Bold" />
        <TextBlock Grid.Column="1" Grid.Row="0"  x:Name="tagTitleDisplay" FontSize="16"></TextBlock>
        <TextBlock Grid.ColumnSpan="2" Grid.Row="1"  x:Name="tagDisplayMessage" FontSize="16"></TextBlock>
    </Grid>
</UserControl>
